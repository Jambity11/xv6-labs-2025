# lab 1 Utilities

我们要做的是一个有关操作系统的实验，不过整个操作系统是为了上层的用户程序服务的，我们第一个实验就把整个os内核当做封装好了，来看看在这样一个内核中如何添加用户程序

## sleep

在`sleep.c`里改，然后在`Makefile`的`UPROGS`里登记`$U/_sleep`(不然程序编译后不会写进`fs.img`，在xv6里将找不到这个命令)

同时程序里还用到了`pause()`这个系统调用


## sixfive

流程同上，只是程序复杂了点

## memdump

略

## finds

写一个程序，同时对目录加深认识

打印所有文件名等于 name 的文件的完整路径

`-exec`

fork 创建子进程，对每个满足的路径执行程序，执行的程序名之前输入中给出
