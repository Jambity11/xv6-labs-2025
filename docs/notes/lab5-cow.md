# lab5 Copy-on-write fork

## Copy-on-Write fork (hard)

> fork() 的语义是创建和父进程几乎一样的子进程,把父进程的所有用户物理页逐页复制一份给子进程。
>
> 问题是：在 shell 里敲命令这种最常见的场景，fork 出来的子进程紧接着就 exec()，复制的内存会立即被替换掉
>
> exec就是在一个进程里抛弃之前的，执行一段新程序
 
 
整个实验就三件事：fork 时共享、写时复制、退出时按引用计数释放











