#import "../templates/lab-report.typ": part

= Lab4: Traps

对应源码分支：#link("https://github.com/Jambity11/xv6-labs-2025/tree/traps")[https://github.com/Jambity11/xv6-labs-2025/tree/traps]

一个正在运行的程序，可能随时被打断：它自己发起系统调用、执行出错（除零、访问非法地址）、或者硬件来了个时钟中断。被打断后它必须立刻停下手里的活、进内核处理，处理完还要原样回到刚才被打断的位置继续跑，仿佛什么都没发生过。内核是怎么做到「停得下、记得住、回得来」的？这是本实验要回答的核心问题。

这个问题不是无关紧要的细节。操作系统的一切「受控」都建立在这套机制上：没有它，一次系统调用、一个异常、一个时钟 tick 都会让程序崩溃，或者让 CPU 直接失控。所以理解 trap，是理解操作系统「如何管理程序」的关键一步。

直觉上，答案并不复杂：进内核之前，先找个地方把当前状态存下来（哪些寄存器、下一条指令在哪），处理完再照着存下来的状态原样恢复。真正需要搞清楚的，是这个「现场」由什么组成、存在哪里、怎么存、怎么恢复。本实验的三个任务正好从三个层面拆开这个问题：RISC-V assembly 看「现场由哪些寄存器和指令组成」，Backtrace 看「内核自己是怎么记录调用现场的」，Alarm 看「怎么主动改写现场、再把它恢复回来」。

#part("前置知识")

*一次 trap 的完整过程。*处理器在执行中遇到「需要内核介入」的事件——系统调用、异常、中断——就会 trap。硬件先把程序计数器切到 `stvec` 寄存器指定的入口（xv6 设成 trampoline 里的 `uservec`），切换到内核态；内核随后把用户寄存器保存到一个叫 `trapframe` 的结构里（每个进程一份，位于该进程页表顶部），处理完事件，再从 `trapframe` 恢复寄存器，用 `sret` 回到用户态、从被打断处继续。所以 `trapframe` 是「现场」的载体，是整个机制的中枢。

*现场由哪些寄存器组成。*RISC-V 调用约定规定：函数参数放在 `a0` 到 `a7`，返回值放在 `a0`；`ra` 存返回地址，`fp`（即 `s0`）是栈帧指针。一次 `jal`/`jalr` 调用会同时把下一条指令地址写进 `ra`，被调函数结束时执行 `ret`，处理器就跳回 `ra` 指定的地方。这些寄存器就是「现场」的具体内容——trap 要保存恢复的，主要就是它们。

*出错的三个线索。*异常发生时，硬件会把信息写进三个寄存器：`scause` 说明「发生了什么」（比如 load page fault、store page fault、时钟中断），`stval` 给出「相关的地址」（缺页时是出错虚拟地址），`sepc` 指向「触发异常的那条指令」。它们是后面所有 page fault 处理（lab5 的 COW、lab9 的 mmap）都要依赖的线索。

*内核栈帧。*函数调用在栈上留信息：xv6 用 `fp` 指当前栈帧，`fp - 8` 存当前函数的返回地址，`fp - 16` 存上一层函数的 `fp`。这样顺着 `fp` 一路往上，就能回溯出调用链——这是 Backtrace 任务的基础。

*去哪查更详细。*《xv6》教材第 4 章「Traps and system calls」；RISC-V 规范里 `ecall`、`sret`、`stvec`、`scause`、`sepc` 的定义；xv6 里看 `kernel/trampoline.S`（切态的汇编）、`kernel/trap.c`（`usertrap`）、`kernel/proc.h` 里的 `struct trapframe`。关键词：「RISC-V 调用约定」「xv6 trapframe」「scause stval sepc」。

== RISC-V assembly (easy)

第一个任务不看内核，先看最底层的现场长什么样。读 `user/call.c` 和它编译出来的 `user/call.asm`，回答 `answers-traps.txt` 里的一串问题。目的很直接：高级语言里一次普通的函数调用，落到汇编层面，到底是怎么用寄存器和指令表达的？

几个关键结论。参数放在 `a0` 到 `a7`，所以 `printf("%d %d\n", 12, 13)` 里，格式串地址进 `a0`、12 进 `a1`、13 进 `a2`——题目问的 13 就在 `a2`。`jal`/`jalr` 跳转的同时把返回地址写进 `ra`，函数末尾 `ret` 再跳回 `ra`。还有字节序：RISC-V 是 little-endian，一个多字节整数的最低字节放在最低地址，所以整数 `0x00646c72` 在内存里是 `72 6c 64 00`，按字符串读就是 `"rld"`；而 57616 用 `%x` 打印是 `e110`，合起来那段经典输出是 `He110 World`。

这里有个必须提醒自己的坑：C 源码里写的函数，汇编里不一定有对应的调用指令。`f()`、`g()` 逻辑太短，编译器可能直接内联、提前算好结果，所以 `call.asm` 里可能根本没有对它们的 `jal`。答案必须以 `call.asm` 实际生成为准，不能照 C 源码猜。

另一个值得记的结论：`printf("x=%d y=%d", 3)` 里第二个 `%d` 没有对应实参，`printf` 仍会照约定去读下一个参数位置，读到的值由不得你——这是 C 可变参数函数的未定义行为，不是 xv6 的错。它说明「调用约定」只是双方约定，函数自己无法检查参数够不够。

== Backtrace (moderate)

有了寄存器和栈帧的知识，第二个任务做一件很实用的东西：让内核能打印「到达当前位置之前的调用链」。内核 panic 时，光看报错那一行往往不知道发生了什么，得知道是哪些函数一层层调过来的。GDB 里能看到的调用栈，原理就是读栈帧——这个任务就是自己实现一遍。

核心思路：当前 `fp` 已知，`fp - 8` 存返回地址，`fp - 16` 存上层 `fp`，于是循环「读返回地址 → 拿上层 fp → 读返回地址 → …」就能一直往上回溯。实现上先加一个 `r_fp()`（读 `s0` 寄存器），然后 `backtrace()` 里循环打印返回地址，直到 `fp` 走出当前内核栈页。

有两个容易忽视的点。一是终止条件不能靠「fp 是否为 0」——内核栈只占一页，越界读会读到垃圾甚至再触发异常，所以要先算好当前栈页的上下边界，每轮检查 `fp` 还在不在页内。二是打印出来的是内核虚拟地址，看不出函数名，要用 `addr2line -e kernel/kernel` 把地址翻译成源文件和行号，才能确认这条链确实是 `bttest` 触发出来的，而不是偶然读到了合法范围内的地址。

这个任务代码不多，但它把「函数调用在栈上留了什么」这件事彻底落实了：每层调用留两个信息（返回地址、上一层栈帧），回溯就是不断读这两个信息。

== Alarm (hard)

第三个任务把 trap 现场从「只能看」变成「可以改」。要求是：用户程序调用 `sigalarm(interval, handler)` 后，进程每累计运行 `interval` 个时钟 tick，内核就让它执行一次用户态 `handler`；handler 调用 `sigreturn()` 后，程序继续从中断前的位置往下跑，寄存器值保持不变。

初看这是个「定时回调」，难点其实在控制流。时钟中断进内核后，`trapframe` 里保存着被打断时的现场，其中 `epc` 是下一条要执行的指令。要「改道」去跑 handler，只要把 `epc` 改成 handler 的地址，内核正常返回用户态时，处理器就会从 handler 开始执行。但改之前必须先把整个 `trapframe` 备份一份——否则 handler 跑完，原来的现场已经丢了，就回不到中断点了。`sigreturn()` 做的就是把备份的现场原样写回 `trapframe`，于是返回时一切如初。

```text
用户程序正常执行
  -> 时钟中断进入 usertrap()
  -> 备份当前 trapframe
  -> 把 epc 改成 handler 地址
  -> 返回用户态执行 handler
  -> handler 调用 sigreturn()
  -> 恢复原 trapframe
  -> 从中断前的位置继续执行
```

实现中我踩到两个典型问题。一个是重入：handler 执行期间若又累计到 interval、再次改 `epc` 保存现场，第二次会把第一次的备份覆盖掉，就再也回不去了。所以要在进程里加一个「handler 正在执行」的标志，期间不再触发。另一个是 `a0`：普通系统调用会把返回值写回用户态 `a0`，而 `sigreturn()` 恢复现场时若直接返回固定值，`a0` 会被分发代码覆盖，导致「寄存器保持不变」的测试失败——正确做法是让 `sys_sigreturn()` 返回原现场里保存的 `a0`。这两个问题都指向同一件事：改 trap 返回路径时，得想清楚系统调用框架接下来还会对寄存器做什么。

== 实验结果

完成三个任务后，在 `traps` 分支运行：

```text
$ make grade
```

`answers-traps.txt`、backtrace、alarmtest 的四个子测试、`usertests -q` 和运行时间检查均通过。

#figure(
  image("../assets/traps/grade.png", width: 92%),
  caption: [Traps Lab 的 make grade 测试结果],
)

三个任务把 trap 从三个层面拆开：指令与寄存器决定「现场由什么组成」，栈帧决定「内核怎么记录调用现场」，trapframe 决定「现场怎么保存、改写和恢复」。它们合起来回答了开头那个问题——内核之所以能「停得下、记得住、回得来」，靠的就是这一整套对执行现场的记录与恢复。
