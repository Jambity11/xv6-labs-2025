#import "../templates/lab-report.typ": part, todo

= Lab1: Utilities

对应源码分支：#link("https://github.com/Jambity11/xv6-labs-2025/tree/util")[https://github.com/Jambity11/xv6-labs-2025/tree/util]

一个操作系统光有内核是不够的，还得有能在它上面跑的用户程序——`ls`、`sleep`、`find` 这些。所以第一个正式实验要解决的问题是：怎么给 xv6 写用户程序？这些程序怎么拿命令行参数、怎么调用系统调用、怎么读文件、怎么遍历目录？

这六个问题看似琐碎，其实是后面所有实验的基础接口。从「用户态」这一侧认识 xv6，就是从这些最普通的工具程序开始的。

初步想法是由易到难、逐个击破：先写最简单的 `sleep`（拿一个参数、调一个系统调用），再写需要状态的文本扫描、按类型解释内存、递归遍历目录，最后是 `fork`/`exec` 组合。每个程序都不长，但各自覆盖一块关键机制。

#part("前置知识")

*用户程序怎么拿参数。*C 的入口是 `main(int argc, char *argv[])`：`argc` 是参数个数，`argv` 是字符串数组。在 shell 里敲 `sleep 10`，`argv[0]` 是 `"sleep"`、`argv[1]` 是 `"10"`。注意 `argv[1]` 是字符串，不是数字，要用 `atoi()` 转换。

*用户态怎么调内核。*用户程序通过系统调用请求内核服务——`open`、`read`、`write`、`fork`、`exec`、`wait`、`exit` 等。它们在 `user/user.h` 里声明，背后由 `usys.pl` 生成进入内核的 stub（细节在 lab2 展开）。用户程序能直接用的，就是这一组受控接口。

*文件与目录。*xv6 里目录也是文件，内容是一串目录项（`struct dirent`），每个是「名字 + inode 号」。`fstat()` 能区分当前路径是普通文件还是目录。这是 `find` 任务的基础。

*进程三板斧。*`fork()` 复制出当前进程的子进程，`exec()` 用新程序替换当前进程，`wait()` 让父进程等子进程结束。关键是 `exec()` 会「替换」调用者自己，所以想「执行完一个命令再继续做别的事」，就得先 `fork` 个子进程去 `exec`，父进程 `wait`。

*去哪查更详细。*《xv6》教材第 1 章「Operating system interfaces」；xv6 里看 `user/*.c` 和 `user/user.h`。关键词：「xv6 user program」「xv6 fork exec wait」「xv6 dirent」。

== Boot xv6 (easy)

动手写代码之前，先解决一个更基本的问题：怎么确认 xv6 能正常启动、能接受命令？在 `util` 分支执行 `make qemu`，启动日志末尾出现 `init: starting sh` 和 `$`，就说明已经进入 xv6 的 shell 了。

这里最容易混淆的是：WSL 的 shell 和 xv6 的 shell 都用 `$` 当提示符，怎么区分？看 `ls` 的输出就行——在 xv6 里 `ls` 列出的是 `fs.img` 里的那几个用户程序，而不是 WSL 目录里的东西。这个任务不改源码，它验证的是「工具链、QEMU、文件系统镜像」这条链是不是通的。

== sleep (easy)

`sleep 10` 要做的：拿参数、把字符串 `"10"` 转成整数、调系统调用等 10 个 tick、退出。代码很短，但它完整地走了一遍「用户命令」的路径：shell 组织参数 → 用户程序解析参数 → 系统调用进内核 → 内核等待后返回。

两个容易踩的点。一是必须检查 `argc`：用户漏传参数时，直接读 `argv[1]` 会访问到不存在的参数，应该打印用法、返回非零。二是改完 `user/sleep.c` 别忘了在 Makefile 的 `UPROGS` 里登记 `$U/_sleep`——漏了这一步，程序即使编译了也不会被写进 `fs.img`，在 xv6 里根本找不到这个命令。

== sixfive (moderate)

这个任务要求读文件、找出所有能被 5 或 6 整除的「独立十进制整数」。难的不是整除判断，而是「什么算一个独立整数」：`/6,` 里的 6 要算，`xv6` 里的 6 不能算。

核心是引入状态机。程序逐字符读，用一个状态记住「现在处于什么上下文」：`SEP_STATE`（刚过分隔符，可以开始一个整数）、`NUMBER_STATE`（正在读一个合法整数）、`INVALID_STATE`（处于字母等非法片段中，忽略到下一个分隔符）。读到 `xv6 /18,` 时：`x` 让状态进 `INVALID`，后面的 `6` 被忽略；空格和 `/` 把状态拉回 `SEP`，接着 `1`、`8` 组成 18，逗号结束这个整数，再判断 18 能否被 5 或 6 整除。

还有一个边界：文件末尾可能没有分隔符，循环结束后要补一次检查，否则最后一个整数会被漏掉。这个任务让我体会到，很多解析程序的本质就是「保存当前字符之前的上下文」，状态机就是干这个的。

== memdump (easy)

`memdump(fmt, data)` 按格式字符串解释一段内存。它讲的是一个很根本的道理：内存里存的只是字节，类型不是内存自带的，而是「读它的代码」决定的——同一块内存，按 `int` 读是整数，按 `char *` 读是字符串，按指针读是地址。

最妙的对比是 `s` 和 `S`。`S` 表示「data 指向的位置本身就是字符串内容」；`s` 表示「data 指向的位置存的是一个地址，要先读这个地址，再按地址去找字符串」。处理完 `S` 要越过字符串本体和结尾的 `\0`，处理完 `s` 只要越过一个指针的大小。把这两者搞混，打印出来的就会是指针字节当字符、或字符字节当地址的乱码。读完每个字段后 `data` 指针的移动量必须和类型大小一致，否则后面所有字段都从错误位置开始解释。

== find (moderate)

`find` 要从一个路径开始，递归找到名字匹配的文件或目录。它第一次让你亲手面对「目录」的本质：目录不是一个神秘的「文件夹」，而是内容为目录项的文件，可以 `open`、可以 `read`。

思路是：打开当前路径，`fstat` 判断类型；比较路径最后一段是否匹配目标；是目录就逐项读目录项、拼出子路径、递归进去。两个坑：一是必须跳过 `.` 和 `..`（分别指向当前目录和父目录），否则会在目录间无限循环；二是目录项只存短文件名，比较时只取路径最后一段（`./a/aa/b` 的匹配名是 `b`），子路径要由「父路径 + 斜杠 + 目录项名」拼接出来。

== find -exec (moderate)

在 `find` 基础上加 `-exec`：找到匹配后执行用户指定的命令，把匹配路径作为最后一个参数（`find . wc -exec echo hi` 找到 `./wc` 后，实际执行 `echo hi ./wc`）。

这里藏着一个经典的坑：`find` 不能自己直接 `exec()`，因为 `exec` 会用新程序替换当前进程，目录搜索在第一次匹配后就停了。正确姿势是 fork-exec-wait——每次匹配 `fork` 一个子进程去 `exec`，父进程 `wait` 等它结束，然后继续遍历。这个任务第一次把「fork 保留继续跑、exec 替换、wait 收尾」这个组合用活了。要让子目录里的匹配也能执行命令，递归调用时还得把 `-exec` 的模式和参数一路传下去。

== 实验结果

完成本 Lab 后，在 `util` 分支运行：

```text
$ make grade
```

也可以配 `GRADEFLAGS` 分别检查 `sleep`、`sixfive`、`memdump`、`find` 和 `exec`，各项最终均通过。

#figure(
  image("../assets/util/grade.png", width: 88%),
  caption: [Utilities Lab 的 make grade 测试结果],
)

本实验完成了 xv6 用户程序从源码、构建到运行的完整过程。几个任务分别用到了系统调用、状态机、指针类型转换、目录递归和 `fork`-`exec`-`wait`，这些机制也是后面所有内核实验所使用的基础接口。
